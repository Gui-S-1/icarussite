<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permitir tráfego HTTPS (padrão seguro) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Permitir localhost para desenvolvimento -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
    
    <!-- Domínios de produção (HTTPS obrigatório) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">trycloudflare.com</domain>
        <domain includeSubdomains="true">icarus.com.br</domain>
    </domain-config>
</network-security-config>
